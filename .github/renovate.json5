{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",

  extends: ["config:best-practices"],

  timezone: "Etc/UTC",
  schedule: ["before 3am on saturday"],

  enabledManagers: ["regex", "nix", "github-actions"],

  regexManagers: [
    {
      fileMatch: ["\\.nix$"],
      matchStrings: [
        // extract depName and currentValue from image strings
        'image\\s*=\\s*"(?<depName>[^:"]+):(?<currentValue>[^"]+)"',
      ],
      datasourceTemplate: "docker",
      versioningTemplate: "loose", // THIS is allowed inside regexManagers
    },
  ],

  packageRules: [
    {
      matchDatasources: ["docker"],
      commitMessageTopic: "Docker image",
    },
  ],
}
